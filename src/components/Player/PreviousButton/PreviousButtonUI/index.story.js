import React from 'react';
import { storiesOf } from '@storybook/react';
import PreviousUI from './index';

storiesOf('PreviousUI Track', module)
  .addWithJSX('default', () => (
    <PreviousUI
      className=""
      onClick={() => {}}
    />
  ));
